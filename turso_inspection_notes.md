# Turso inspection — no changes applied

تم الاتصال بقاعدة Turso بنجاح باستخدام بيانات الاتصال التي أرسلها المستخدم، دون إنشاء أو حذف أو تعديل أي جدول.

النتيجة: قاعدة Turso ليست فارغة، وتحتوي على 41 جدولًا وبيانات موجودة. من أمثلة الجداول الحالية: `AnalyticsEvent`, `AuthSession`, `Customer`, `Domain`, `Order`, `Product`, `Site`, `User`, `users`, `settings`, وغيرها.

يوجد تعارض مباشر مع مشروع smmnine:

- جدول `users` موجود مسبقًا لكن بنيته مختلفة جذريًا: يحتوي `id TEXT`, `provider`, `external_id`, `access_token`, `refresh_token`, `scope`, `username`, `email`, `password_hash`، ولا يحتوي `balance`, `role`, `is_banned` وغيرها التي يحتاجها smmnine.
- جداول smmnine الأساسية `orders`, `transactions`, `providers`, `provider_services`, `api_keys` غير موجودة.
- جدول `settings` موجود مسبقًا ببنية مختلفة (`id`, `user_id`, `key`, `value`...).
- توجد جداول أخرى بأسماء متقاربة مثل `User` و`Order` لكنها تخص مخططًا مختلفًا.

لم يتم تعديل المشروع أو قاعدة Turso حتى الآن. يلزم قرار المستخدم قبل أي عملية قد تكون مدمرة:

1. الحفاظ على كل البيانات الحالية وإنشاء جداول smmnine بأسماء معزولة/بادئة، ثم تعديل المشروع ليتعامل معها؛ أو
2. اعتبار قاعدة Turso مخصصة لـ smmnine وحذف/إعادة تهيئة الجداول الحالية بعد تأكيد صريح، مع احتمال فقدان البيانات الموجودة.

ملاحظة أمنية: رمز Turso ظهر في رسالة المستخدم، ويجب تدويره من لوحة Turso بعد الانتهاء من الإعداد أو فورًا إذا كان مستخدمًا في مكان آخر. لا يجب حفظه في Git أو ملفات مرفوعة.
