# حزمة تسليم smmnine للمطوّر

هذه الحزمة تحتوي على نسخة المشروع المصدرية من فرع النشر، بما في ذلك تصميم Royal Gold، واجهات المستخدم، API v2، تكامل Turso، إعدادات الخدمات والمزودين، التوثيق الكامل، ومعرض صور المتجر العام.

## محتويات الحزمة

| المكوّن | الحالة |
|---|---|
| كود Next.js وTypeScript | مضمّن بالكامل |
| الواجهات والتصميم | مضمّنة بالكامل |
| API ومسارات الخادم | مضمّنة بالكامل |
| مخطط قاعدة البيانات وملفات التهيئة | مضمّنة ضمن `src/lib/db.ts` و`scripts/` |
| إعدادات الاتصال المحلية | موجودة داخل `.env.local` داخل الأرشيف المشفّر فقط |
| معرض صور المتجر | موجود داخل `docs/store-gallery/` |
| بيانات المستخدمين والأرصدة والطلبات الحية | ليست نسخة مستقلة؛ الاتصال يتم بقاعدة Turso المحددة في البيئة |

## طريقة الاستخدام

بعد فك تشفير الأرشيف، انتقل إلى مجلد المشروع ثم نفّذ:

```bash
pnpm install
pnpm exec tsc --noEmit
pnpm build
pnpm start -- -p 3000
```

ملف `.env.local` الموجود داخل الأرشيف يحتوي على إعدادات البيئة اللازمة للاتصال المحلي. يجب عدم رفعه إلى GitHub أو إرساله في قنوات عامة. عند النشر على Vercel، يجب إدخال القيم يدويًا في Environment Variables بدل رفع الملف.

## قاعدة البيانات

المشروع يستخدم Turso/libSQL من خلال `@libsql/client`. الاتصال بقاعدة البيانات لا يعتمد على SQLite المحلية في بيئة الإنتاج عندما تكون متغيرات Turso مفعلة. ملفات المشروع تحتوي على `initDb()` وملفات الترحيل والتهيئة اللازمة لإنشاء الجداول أو تحديثها.

هذه الحزمة لا تنسخ سجلات المستخدمين أو الأرصدة أو الطلبات إلى ملف SQL منفصل. عند تشغيل المشروع باستخدام إعدادات Turso الصحيحة، يقرأ المشروع قاعدة البيانات الحية مباشرة. لا تشغّل أي seed أو reset على قاعدة الإنتاج قبل مراجعة المطوّر.

## الدفع والشبكات

إعدادات طرق الدفع وعناوين الشبكات الرسمية محفوظة في قاعدة البيانات ويقرأها مسار الإيداع من Turso. يجب مطابقة العملة مع الشبكة دائمًا، وعدم تغيير العناوين أو إرسال أموال حقيقية أثناء الاختبار. تكامل NOWPayments يحتاج مفاتيح جديدة غير المفاتيح التي ظهرت سابقًا، ويجب ضبطها في Vercel كأسرار فقط بعد تدويرها.

## ملاحظات أمنية

هذه الحزمة مشفرة. كلمة مرور فك التشفير موجودة في ملف منفصل عن الأرشيف. لا ترفع الأرشيف أو ملف كلمة المرور إلى GitHub، ولا ترسل الملفين في مكان عام. بعد استلام المطوّر للحزمة، غيّر أسرار Turso وSession وNOWPayments إذا تم تداولها خارج القناة الآمنة.

## روابط المشروع

- المستودع الأساسي: https://github.com/wahaca9693/smmnine-landing
- النسخة الاحتياطية: https://github.com/wahaca9693/smmnine-backup
- فرع التسليم: `deploy/royal-gold-crypto-turso-20260817`
- معرض المتجر العام: `docs/store-gallery/README.md`

**مهم:** لا تعتبر وجود ملف البيئة وحده دليلًا على أن الدفع الحقيقي جاهز للإنتاج. يجب اختبار webhook والتوقيع وidempotency في بيئة معاينة دون أموال حقيقية قبل تفعيل الإنتاج.
