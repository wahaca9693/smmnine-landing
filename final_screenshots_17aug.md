# اللقطات النهائية — 17 أغسطس (آخر حالة)

## لقطة 13_admin_providers_final.png — ممتازة
لوحة مزودي الخدمات تعرض: بطاقة «مزود تجريبي» مع شارة ذهبية، روابط الاتصال، تنبيه فشل الاتصال بالمزود، صفوف خدمات «جديد» (فيسبوك 30$ هامش، متابعين إنستغرام — عرب)، أزرار الهامش/تحديث أسعار الكل/مزامنة/استعراض خدماته. كل شيء يعمل.

## لقطة 14_admin_providers_scroll_final.png — نفس الصفحة بعد التمرير (لم تُفحص بعد)

## تم إنجازه في هذا البند
1. إصلاح خطأ 500 في /api/v2/route.ts (إزالة عمود via غير الموجود + استرجاع الرصيد عند فشل الطلب + تسجيل معاملة خصم)
2. إعادة ضبط كلمة مرور الأدمن عبر سكربت bcrypt في /home/ubuntu/reset_admin_password.py (كلمة المرور: Admin123456!)
3. User API يعمل بالكامل: مفتاح smm-...، استقطاع من محفظة المستخدم، طلب مسجل
4. التقاط يتم دائمًا عبر تسجيل الدخول داخل Playwright (نموذج login بplaceholders: "أدخل اسم المستخدم") وليس كوكي curl (HttpOnly تنتهي قريبًا/لا تُرسل)

## ملفات مهمة
- لقطات: /home/ubuntu/shots_api/ (01..04 ممتازة، 13 ممتازة)
- جلسة الدخول: تسجيل دخول من داخل الصفحة يوجه إلى /services (login page بها isLogin mode)
- قاعدة البيانات: /home/ubuntu/smmnine-data/local.db
- الخادم: cd /home/ubuntu/smmnine && source /home/ubuntu/.user_env && USE_LOCAL_DB=1 LOCAL_DB_PATH=/home/ubuntu/smmnine-data/local.db pnpm dev

## المتبقي
- فحص لقطة 14 (التمرير)
- تسليم نهائي للمستخدم: User API + ربط المزودين يعملان
