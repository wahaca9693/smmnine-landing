# نتيجة معاينة Turnstile

التاريخ: 2026-08-18

تم تشغيل نسخة الإنتاج المحلية بعد إعادة البناء، ونجح `/api/health` باستجابة HTTP 200.

عند فتح `/login` ثم اختيار «إنشاء حساب»، ظهر عنصر Cloudflare Turnstile داخل نموذج التسجيل بحالة `Verifying...`، مع روابط Cloudflare الخاصة بالخصوصية والمساعدة. هذا يؤكد أن `NEXT_PUBLIC_TURNSTILE_SITE_KEY` مقروء في الواجهة وأن الودجت ليس مجرد مساحة فارغة.

لم يتم إدخال بيانات شخصية أو إرسال نموذج أو إنشاء حساب أثناء المعاينة. يبقى التحقق الخادمي معتمدًا على `TURNSTILE_SECRET_KEY` داخل متغيرات البيئة فقط، ويجب تدوير المفتاح السري الذي ظهر في رسالة المستخدم قبل استخدامه في الإنتاج.

تم إنهاء أي بناء عالق وإعادة بناء Next.js بنجاح قبل تشغيل الخادم الحالي.
